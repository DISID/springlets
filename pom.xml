<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>io.spring.platform</groupId>
    <artifactId>platform-bom</artifactId>
    <version>Athens-RC1</version>
  </parent>
  <groupId>com.disid</groupId>
  <artifactId>springlets</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>Springlets</name>
  <description>Springlets provides a series of heterogeneus small utilities and Spring Boot starters not available in the main Spring projects: Spring Framework, Spring Data, Spring Boot, ...</description>
  <organization>
    <name>DISID Corporation</name>
    <url>http://www.disid.com</url> 
  </organization>
  <properties>
    <java.version>1.7</java.version>
    <!--tracee.version>1.1.2</tracee.version-->
  </properties>

  <modules>
    <module>spring-framework</module>
    <module>spring-data</module>
    <module>spring-boot-starters</module>
  </modules>

  <dependencyManagement>
    <!--
    = Dependencies order:

    * Two dependency groups: first spring dependencies and next the external ones.
    * Inside a group: alphabetical order, ordering first by groupId, then by artifactId.
     -->
    <dependencies>

      <!-- TracEE
      <dependency>
        <groupId>io.tracee.binding</groupId>
        <artifactId>tracee-jaxws</artifactId>
        <version>${tracee.version}</version>
      </dependency>

      <dependency>
        <groupId>io.tracee.binding</groupId>
        <artifactId>tracee-cxf</artifactId>
        <version>${tracee.version}</version>
      </dependency>

      <dependency>
        <groupId>io.tracee.binding</groupId>
        <artifactId>tracee-jms</artifactId>
        <version>${tracee.version}</version>
      </dependency>

      <dependency>
        <groupId>io.tracee.binding</groupId>
        <artifactId>tracee-springmvc</artifactId>
        <version>${tracee.version}</version>
      </dependency>
      -->

    </dependencies>
  </dependencyManagement>

</project>
